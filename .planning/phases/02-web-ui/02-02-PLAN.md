---
phase: 02-web-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/templates/book.html
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-05
user_setup: []

must_haves:
  truths:
    - "Highlights section appears on book detail page after reading stats"
    - "Each highlight shows text, and conditionally shows note/page/chapter"
    - "Empty state message displays when no highlights exist"
    - "No edit/delete controls are present (read-only display)"
  artifacts:
    - path: "web/templates/book.html"
      provides: "Highlights section with all required display elements"
      contains: "highlights-section"
  key_links:
    - from: "web/templates/book.html"
      to: "$.highlights"
      via: "template range"
      pattern: "\\{\\{\\s*range\\s*\\.\\s*\\}\\}"
---

<objective>
Add a highlights section to the book detail template that displays synced highlights with their text, notes, page, and chapter information in a read-only format.

Purpose: Provide users with a visual display of their book highlights on the book detail page.
Output: Modified book.html template with highlights section.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-web-ui/02-RESEARCH.md
@.planning/phases/02-web-ui/02-UI-SPEC.md

<interfaces>
<!-- Key template patterns and entity fields -->

From web/templates/book.html (existing stats section pattern):
```html
{{ with $.stats }}
<section class="reading-stats">
    <hgroup>
        <h3>Reading Stats</h3>
    </hgroup>
    <table>
        <!-- stats content -->
    </table>
</section>
{{ end }}
```

From internal/entity/highlight.go:
```go
type Highlight struct {
    Text      string `json:"text" binding:"required"`  // Required
    Note      string `json:"note"`                      // Optional
    Page      string `json:"page"`                      // Optional
    Chapter   string `json:"chapter"`                   // Optional
    Timestamp int64  `json:"time"`
}
```

UI-SPEC.md copywriting contract:
- Section heading: "Highlights" with count subtitle
- Empty state heading: "Highlights"
- Empty state body: "No highlights synced yet. Use KOReader to highlight text in this book."

Template patterns to use:
- `{{ with $.highlights }}` for conditional rendering
- `{{ range . }}` for iterating over highlights
- `{{ with .Note }}` for conditional note display
- `{{ with .Page }}` for conditional page display
- `{{ len . }}` for count display
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add highlights section to book.html template</name>
  <files>web/templates/book.html</files>
  <read_first>
    - web/templates/book.html (current template structure, stats section placement)
    - internal/entity/highlight.go (Highlight struct fields: Text, Note, Page, Chapter)
    - .planning/phases/02-web-ui/02-UI-SPEC.md (copywriting contract for exact text)
  </read_first>
  <behavior>
    - Test: Template contains "highlights-section" class
    - Test: Template uses `{{ with $.highlights }}` for conditional
    - Test: Template ranges over highlights with `{{ range . }}`
    - Test: Template displays .Text for each highlight
    - Test: Template conditionally displays .Note using `{{ with .Note }}`
    - Test: Template conditionally displays .Page and .Chapter
    - Test: Template has empty state with exact copy from UI-SPEC
    - Test: No form elements, input, or button elements in highlights section
  </behavior>
  <action>
    Modify web/templates/book.html:

    Add the highlights section AFTER the reading stats section (after line 93, before the final `{{ end }}`).

    Insert the following template section:

    ```html
    <!-- Highlights section -->
    {{ with $.highlights }}
    <section class="highlights-section">
        <hgroup>
            <h3>Highlights</h3>
            <p>{{ len . }} highlights synced from KOReader</p>
        </hgroup>
        {{ range . }}
        <article class="highlight-card">
            <blockquote class="highlight-text">
                {{ .Text }}
            </blockquote>
            {{ with .Note }}
            <div class="highlight-note">
                <strong>Note:</strong> {{ . }}
            </div>
            {{ end }}
            <footer class="highlight-meta">
                {{ with .Page }}<span>Page {{ . }}</span>{{ end }}
                {{ with .Chapter }}<span> / {{ . }}</span>{{ end }}
            </footer>
        </article>
        {{ end }}
    </section>
    {{ else }}
    <section class="highlights-section">
        <hgroup>
            <h3>Highlights</h3>
            <p>No highlights synced yet. Use KOReader to highlight text in this book.</p>
        </hgroup>
    </section>
    {{ end }}
    ```

    PLACEMENT: Insert this AFTER the `{{ with $.stats }}...{{ end }}` block (line 93) and BEFORE the final `{{ end }}` (line 94).

    CRITICAL REQUIREMENTS (UI-05 - Read-only display):
    - NO `<form>` elements
    - NO `<input>` elements
    - NO `<button>` elements with edit/delete actions
    - NO `<a>` links to edit/delete
    - Only display elements: section, article, blockquote, div, footer, span
  </action>
  <verify>
    <automated>grep -c "highlights-section" web/templates/book.html | xargs -I {} sh -c 'test {} -ge 2 && echo PASS || echo FAIL'</automated>
  </verify>
  <done>
    - Highlights section added after reading stats section
    - Uses semantic HTML: section, article, blockquote, footer
    - Conditionally displays note with `{{ with .Note }}`
    - Conditionally displays page/chapter with `{{ with .Page }}` and `{{ with .Chapter }}`
    - Empty state shows exact message: "No highlights synced yet. Use KOReader to highlight text in this book."
    - No edit/delete controls present (read-only)
  </done>
</task>

</tasks>

<verification>
Verify template structure:
- `grep -c "highlights-section" web/templates/book.html` returns 2 or more
- `grep -c "highlight-card" web/templates/book.html` returns 1 or more
- `grep -c "{{ with $.highlights }}" web/templates/book.html` returns 1
- `grep -c "{{ range . }}" web/templates/book.html` returns 2 or more (stats + highlights)
- `grep "type=\"submit\"" web/templates/book.html` does NOT show anything in highlights section
</verification>

<success_criteria>
- Highlights section displays on book detail page
- Each highlight shows text, note (if present), page/chapter (if present)
- Empty state displays helpful message when no highlights
- No editing controls present (purely read-only display)
- Template follows existing pattern (hgroup with h3 heading)
</success_criteria>

<output>
After completion, create `.planning/phases/02-web-ui/02-02-SUMMARY.md`
</output>
